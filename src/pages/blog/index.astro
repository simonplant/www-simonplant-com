---
import Base from '../../layouts/Base.astro';
import { getPublishedCollection } from '../../content/_helpers';

const entries = await getPublishedCollection('commentary');
const byDateDesc = (a: typeof entries[number], b: typeof entries[number]) =>
  b.data.publishedDate.getTime() - a.data.publishedDate.getTime();
const pinned = entries.filter((e) => e.data.pinned).sort(byDateDesc);
const sorted = entries.filter((e) => !e.data.pinned).sort(byDateDesc);

const allTags = [...new Set(entries.flatMap((e) => e.data.tags))].sort();
---

<Base title="Blog — Simon Plant" description="What it actually takes to run AI agents in production — infrastructure patterns, security decisions, and lessons from someone who did the same for cloud a decade ago." jsonLd={{
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  name: 'Blog — Simon Plant',
  url: 'https://www.simonplant.com/blog',
  description: 'What it actually takes to run AI agents in production — infrastructure patterns, security decisions, and lessons from someone who did the same for cloud a decade ago.',
}}>
  <main class="section-spacing">
    <div class="container-prose">
      <h1 class="font-serif text-4xl text-gray-50 mb-4">Blog</h1>
      <p class="text-text-secondary text-lg mb-8 font-prose">
        What it actually takes to run AI agents in production — infrastructure patterns, security decisions, and lessons from someone who did the same for cloud a decade ago.
      </p>

      {pinned.length > 0 && (
        <aside class="mb-10" aria-label="Start here">
          {pinned.map((entry) => (
            <a
              href={`/blog/${entry.id}`}
              class="group block rounded-lg border border-border-subtle bg-surface-card px-5 py-4 hover:border-primary-600 transition-colors"
            >
              <div class="flex items-baseline gap-2 mb-1">
                <span class="text-xs font-sans uppercase tracking-wider text-primary-400">New here? Start here</span>
              </div>
              <h2 class="font-serif text-xl text-gray-50 mb-1 group-hover:text-primary-400 transition-colors">
                {entry.data.title}
              </h2>
              <p class="text-text-secondary font-prose text-sm leading-relaxed">
                {entry.data.description}
              </p>
            </a>
          ))}
        </aside>
      )}

      {allTags.length > 0 && (
        <nav class="mb-12 flex flex-wrap gap-2" aria-label="Filter by tag">
          <button
            data-tag=""
            class="tag-filter tag-active px-3 py-1 rounded-full text-sm font-sans border border-border-subtle transition-colors"
          >
            All
          </button>
          {allTags.map((tag) => (
            <button
              data-tag={tag}
              class="tag-filter px-3 py-1 rounded-full text-sm font-sans border border-border-subtle text-text-muted hover:text-primary-400 hover:border-primary-600 transition-colors"
            >
              {tag}
            </button>
          ))}
        </nav>
      )}

      {sorted.length === 0 ? (
        <p class="text-text-muted">Nothing published yet. Check back soon.</p>
      ) : (
        <ol class="space-y-8 list-none p-0" id="blog-list">
          {sorted.map((entry) => {
            const wordCount = entry.body ? entry.body.split(/\s+/).length : 0;
            const readingTime = Math.max(1, Math.round(wordCount / 230));
            return (
              <li class="blog-item border-b border-border-subtle pb-8 last:border-b-0" data-tags={entry.data.tags.join(',')}>
                <a href={`/blog/${entry.id}`} class="group block">
                  <h2 class="font-serif text-2xl text-gray-50 mb-2 group-hover:text-primary-400 transition-colors">
                    {entry.data.title}
                  </h2>
                  <p class="text-text-secondary font-prose leading-relaxed mb-3">
                    {entry.data.description}
                  </p>
                  <div class="flex flex-wrap items-center gap-2 text-sm font-sans">
                    <time class="text-text-muted" datetime={entry.data.publishedDate.toISOString().slice(0, 10)}>
                      {entry.data.publishedDate.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}
                    </time>
                    <span class="text-text-muted">&middot;</span>
                    <span class="text-text-muted">{readingTime} min read</span>
                    {entry.data.tags.map((tag) => (
                      <span class="text-primary-400 bg-primary-400/10 px-2 py-0.5 rounded-full text-xs">
                        {tag}
                      </span>
                    ))}
                  </div>
                </a>
              </li>
            );
          })}
        </ol>
      )}
    </div>
  </main>
</Base>

<style>
  .tag-active {
    color: var(--color-primary-400);
    border-color: var(--color-primary-600);
    background-color: color-mix(in srgb, var(--color-primary-400) 10%, transparent);
  }
</style>

<script>
  document.addEventListener('astro:page-load', () => {
    const buttons = document.querySelectorAll<HTMLButtonElement>('.tag-filter');
    const items = document.querySelectorAll<HTMLLIElement>('.blog-item');

    buttons.forEach((btn) => {
      btn.addEventListener('click', () => {
        const tag = btn.dataset.tag ?? '';

        buttons.forEach((b) => b.classList.remove('tag-active'));
        btn.classList.add('tag-active');

        items.forEach((item) => {
          const itemTags = (item.dataset.tags ?? '').split(',');
          item.style.display = tag === '' || itemTags.includes(tag) ? '' : 'none';
        });
      });
    });
  });
</script>
